# Dependencias del sistema de agenda clinica
# Instalar con: pip install -r requirements.txt

# API de Anthropic (Claude)
anthropic>=0.40.0

# Servidor web (webhook de WhatsApp + API)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# WhatsApp Meta Cloud API (cliente HTTP)
httpx>=0.26.0

# Scheduler de confirmaciones proactivas
apscheduler>=3.10.0

# Variables de entorno
python-dotenv>=1.0.0

# Tests
pytest>=8.0.0
pytest-cov>=5.0.0
